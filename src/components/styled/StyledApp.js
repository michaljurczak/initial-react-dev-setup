import styled from 'styled-components';

/**
 * Component for styling the app.
 *
 * @component
 * @example
 * return (
 *  <StyledApp>
 *    <Component />
 *   </StyledApp>  
 * )
 */

const StyledApp = styled.h1`
    color:red;
    text-align: center;
`;

export default StyledApp;